{
  "name": "voice2voice-web",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:local": "env $(cat .env.local | grep -v '^#' | xargs) vercel dev",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@openai/agents-realtime": "latest",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}